# Outputs surface resource attributes for human use (CLI inspection) and for
# downstream consumption (other Terraform configs, CI workflows).
#
# Empty for now — we'll add outputs as modules are introduced in Steps 3.5-3.8.
# Examples of what will live here later:
#   - storage_account_name (for Databricks to mount)
#   - databricks_workspace_url (for accessing the UI)
#   - key_vault_uri (for app code to authenticate)
